[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/dist"

[context.production.environment]
  # Set this in Netlify UI instead of committing secrets here
  # VITE_API_URL = "https://your-backend.example.com"
